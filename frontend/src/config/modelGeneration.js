/**
 * Hugging Face chat generation (uses inference credits).
 * Scoring / select-best is unchanged either way.
 *
 * Flip to `true` when you want dual-model testing again.
 */
export const ENABLE_HUGGINGFACE_GENERATION = false;
